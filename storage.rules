rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // No files are served or accepted between tournaments.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
