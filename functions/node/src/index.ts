/**
 * TipParta is dormant between tournaments.
 *
 * This module intentionally exports no Firebase Functions, so deploying the
 * project cannot recreate callable endpoints, HTTP endpoints, or schedulers.
 * The reusable workflow modules remain in this source tree for a future,
 * explicitly reviewed tournament reactivation.
 */
export {};
